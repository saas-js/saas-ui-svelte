import { Meta, Canvas, ArgTypes } from "@storybook/addon-docs/blocks";
import * as SpinnerStories from "./Spinner.stories.svelte";
import { Spinner } from "$saas/components/spinner";
import { spinnerPattern } from "../exclude-patterns";

<Meta of={SpinnerStories} />

# Spinner

## Used to provide a visual cue that an action is processing.

<Canvas of={SpinnerStories.Basic} />

## Usage

```tsx
<script>
	import { Spinner } from "$saas/components/spinner";
</script>

<Spinner />
```

## Examples

### Sizes

Use the `size` prop to change the size of the spinner.

<Canvas of={SpinnerStories.Sizes} />

### Colours

Use the `colour` prop to change the colour palette of the spinner.

<Canvas of={SpinnerStories.Colours} />

### Track Colour

Use the `trackColour` prop to customize the track (background) colour of the spinner.

<Canvas of={SpinnerStories.TrackColour} />

### Custom Speed

Use the `animationDuration` prop to customize the animation speed of the spinner.

<Canvas of={SpinnerStories.CustomSpeed} />

### Thickness

Use the `borderWidth` prop to customize the thickness of the spinner.

<Canvas of={SpinnerStories.Thickness} />

### With Label

Compose the spinner with a label to provide additional context.

<Canvas of={SpinnerStories.WithLabel} />

## Props

<ArgTypes of={Spinner} exclude={spinnerPattern} />
