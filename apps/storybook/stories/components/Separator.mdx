import { Meta, Story, Canvas, ArgTypes } from "@storybook/addon-docs/blocks";
import * as SeparatorStories from "./Separator.stories.svelte";
import { Separator } from "$saas/components/separator";
import { separatorPattern } from "../utils";

<Meta of={SeparatorStories} />

# Separator

## Used to visually separate content.

<Canvas of={SeparatorStories.Basic} />

## Anatomy

```tsx
<script>
	import { Separator } from "$saas/components/separator";
</script>

<Separator />
```

## Examples

### Variants

Use the `variant` prop to change the appearance of the separator.

<Canvas of={SeparatorStories.Variants} />

### Sizes

Use the `size` prop to change the size of the separator.

<Canvas of={SeparatorStories.Sizes} />

### Label

Compose the `Separator` with `Group` and `Text` to add a label.

<Canvas of={SeparatorStories.Label} />

### Vertical

Use the `orientation` prop to change the orientation of the separator.

<Canvas of={SeparatorStories.Vertical} />

### Colours

Use the `colour` prop to change the colour of the separator.

<Canvas of={SeparatorStories.Colours} />

## Props

<ArgTypes of={Separator} exclude={separatorPattern} />
