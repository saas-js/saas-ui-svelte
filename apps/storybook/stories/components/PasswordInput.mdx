import { Meta, Canvas, ArgTypes } from "@storybook/addon-docs/blocks";
import * as PasswordInputStories from "./PasswordInput.stories.svelte";
import { PasswordInput } from "$saas/components/password-input";
import { passwordInputPattern } from "../utils";

<Meta of={PasswordInputStories} />

# Password Input

## Used to collect passwords.

<Canvas of={PasswordInputStories.Basic} />

## Anatomy

```tsx
<script>
	import { PasswordInput } from "$saas/components/password-input";
</script>

<PasswordInput />
```

## Examples

### Sizes

Use the `size` prop to change the size of the password input. The password input sizes are mapped to the Input component sizes.

<Canvas of={PasswordInputStories.Sizes} />

### Controlled

Use the `value` and `bind:value` to control the current value.

<Canvas of={PasswordInputStories.Controlled} />

### Controlled Visibility

Use the `visible` and `onVisibleChange` props to control the visibility of the password input.

<Canvas of={PasswordInputStories.ControlledVisibility} />

### With Field

Here's an example of how to use the PasswordInput component with Field for form layouts.

<Canvas of={PasswordInputStories.WithField} />

### Disabled

Use the `disabled` prop to disable the input.

<Canvas of={PasswordInputStories.Disabled} />

### Invalid

Use the `invalid` prop or wrap in an invalid Field to show an error state.

<Canvas of={PasswordInputStories.Invalid} />

### Colours

Use the `colour` prop to change the focus ring color of the password input.

<Canvas of={PasswordInputStories.Colours} />

## Props

<ArgTypes of={PasswordInput} exclude={passwordInputPattern} />
