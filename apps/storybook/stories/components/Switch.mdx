import {
	Meta,
	Story,
	Canvas,
	ArgTypes,
	Source,
} from "@storybook/addon-docs/blocks";
import * as SwitchStories from "./Switch.stories.svelte";
import { Switch } from "$saas/components/switch";
import { statusPattern } from "../utils";

<Meta of={SwitchStories} />

# Switch

## Used to capture a binary state.

<Canvas of={SwitchStories.Basic} />

## Anatomy

```tsx
<script>
	import { Switch } from "$saas/components/switch";
</script>

<Switch>Label</Switch>
```

## Examples

### Sizes

Use the `size` prop to change the size of the switch component.

<Canvas of={SwitchStories.Sizes} />

### Variants

Use the `variant` prop to change the visual style of the switch.

<Canvas of={SwitchStories.Variants} />

### Colours

Use the `colour` prop to change the colour scheme of the component.

<Canvas of={SwitchStories.Colours} />

### Disabled

Use the `disabled` prop to disable the switch.

<Canvas of={SwitchStories.Disabled} />

### Track Label

Use the `trackLabel` prop to display different label based on the checked state.

<Canvas of={SwitchStories.TrackLabel} />

### Thumb Label

Use the `thumbLabel` prop to add an icon to the switch thumb.

<Canvas of={SwitchStories.ThumbLabel} />

## Props

### Switch

<ArgTypes of={Switch} exclude={statusPattern} />
