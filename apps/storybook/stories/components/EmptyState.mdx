import { Meta, Story, Canvas, ArgTypes } from "@storybook/addon-docs/blocks";
import * as EmptyStateStories from "./EmptyState.stories.svelte";
import { EmptyState } from "$saas/components/empty-state";
import { emptyStatePattern } from "../utils";

<Meta of={EmptyStateStories} />

# Empty State

## Used to indicate when a resource is empty or unavailable.

<Canvas of={EmptyStateStories.Basic} />

## Anatomy

```tsx
<script>
	import { EmptyState } from "$saas/components/empty-state";
	import ShoppingCart from "phosphor-svelte/lib/ShoppingCart";
</script>

<EmptyState
	icon={ShoppingCart}
	title="Your cart is empty"
	description="Explore our products and add items to your cart"
/>
```

## Examples

### With Action

Use `children` to add action buttons or other interactive elements below the description.

<Canvas of={EmptyStateStories.WithAction} />

### With List

Combine with the `List` component to show helpful suggestions.

<Canvas of={EmptyStateStories.WithList} />

### Sizes

Use the `size` prop to change the size of the empty state. Values can be `sm`, `md`, or `lg`.

<Canvas of={EmptyStateStories.Sizes} />

### Alignment

Use the `align` prop to change the content alignment. Values can be `centre`, `start`, or `end`.

<Canvas of={EmptyStateStories.Alignment} />

### Colours

Use the `colour` prop to change the icon colour palette.

<Canvas of={EmptyStateStories.Colours} />

## Props

<ArgTypes of={EmptyState} exclude={emptyStatePattern} />
