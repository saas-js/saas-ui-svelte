import {
	Meta,
	Story,
	Canvas,
	ArgTypes,
	Source,
} from "@storybook/addon-docs/blocks";
import * as StatusStories from "./Status.stories.svelte";
import { Status } from "$saas/components/status";
import { statusPattern } from "../utils";

<Meta of={StatusStories} />

# Status

## Used to indicate the status of a process or state.

<Canvas of={StatusStories.Basic} />

## Anatomy

```tsx
<script>
	import { Status } from "$saas/components/status";
</script>

<Status value="success">Label</Status>
```

## Examples

### Label

Render the label within the status component.

<Canvas of={StatusStories.Label} />

### Sizes

Use the `size` prop to change the size of the status component.

<Canvas of={StatusStories.Sizes} />

### Colours

Use the `colour` prop to customise the indicator colour using any colour palette.

<Canvas of={StatusStories.Colours} />

## Props

### Status

<ArgTypes of={Status} exclude={statusPattern} />
