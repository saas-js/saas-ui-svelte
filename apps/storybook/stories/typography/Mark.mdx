import { Meta, Canvas, ArgTypes } from "@storybook/addon-docs/blocks";
import * as MarkStories from "./Mark.stories.svelte";
import { Mark } from "$saas/typography/mark";
import { markPattern } from "../utils";

<Meta of={MarkStories} />

# Mark

Used to mark text for emphasis.

<Canvas of={MarkStories.Basic} />

## Usage

```tsx
<script>
	import { Text } from "$saas/typography/text";
	import { Mark } from "$saas/typography/mark";
</script>

<Text> The <Mark>design system</Mark> is a collection of UI elements </Text>
```

## Examples

### Variants

Use the `variant` prop to change the visual style of the mark. Available variants are `subtle`, `solid`, `text`, and `plain`.

<Canvas of={MarkStories.Variants} />

```tsx
<Text>
	The <Mark variant="subtle" colour="indigo">design system</Mark> is a collection of UI elements
</Text>
<Text>
	The <Mark variant="solid" colour="indigo">design system</Mark> is a collection of UI elements
</Text>
<Text>
	The <Mark variant="text" colour="indigo">design system</Mark> is a collection of UI elements
</Text>
<Text>
	The <Mark variant="plain" colour="indigo">design system</Mark> is a collection of UI elements
</Text>
```

### Props

<ArgTypes of={Mark} exclude={markPattern} />
