import Root from "./tag.svelte";

export {
    tag,
    tagLabel,
    tagCloseTrigger,
    tagCloseButton,
    tagElement,
    tagAvatar,
    type TagVariants,
} from "./tag.svelte";

export const Tag = Root;
export { Root as TagRoot };
